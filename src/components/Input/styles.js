import { StyleSheet } from "react-native";
import colors from "../../Constants/colors";

const styles = StyleSheet.create({
    input: {
        height: 30,
        borderBottomColor: colors.quinario,
        borderBottomWidth: 2,
        marginVertical: 10,
        width: 100,
    },
});

export default styles;